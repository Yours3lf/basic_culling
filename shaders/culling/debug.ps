#version 430 core

uniform vec3 light;

layout(location=0) out vec4 color;

in vec3 normals;
in vec3 light_dir;

void main()
{
  color = vec4( light * (0.1 + dot( light_dir, normals )), 1 );
  //color = vec4( normals, 1 );
  //color = vec4( 1 );
  //color = vec4( normals, 1 );
}
